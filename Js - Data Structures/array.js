// rm -f .git/index.lock to save from index lock or git lock


// java script i s not tightly coupled arrays can store both int and string or an diff datatype in an single array 

let colors =['blue', 'orange'];
colors[2] = 1;
console.log(colors.length);